<% 
const aktif=parseInt(aktifSayfa) ;
const onceki=aktif-1;
const sonraki=aktif+1;
const toplamSayfa=sayfaSayisi;
%> 
<% if (toplamSayfa>1) { %>
 <nav class="pagination is-medium" role="navigation" aria-label="pagination">
    <a <%=aktif!=1?'href=/blog?page='+onceki:'' %>  class="pagination-previous" <%=aktifSayfa==1?'disabled':''%>>Önceki</a>
    <a <%=aktif!=toplamSayfa?'href=/blog?page='+sonraki:'' %> class="pagination-next"  <%=aktifSayfa==toplamSayfa?'disabled':''%>>Sonraki sayfa</a>
    <ul class="pagination-list">
      <% for( let index = 1; index <=sayfaSayisi; index++ ) { %>
        <li>
          <a href="/blog?page=<%=index %>" class="pagination-link <%=index==aktifSayfa?'is-current':'' %> " aria-label="Goto page"><%-index%> </a>
        </li>
      <% } %>
    </ul>
  </nav>
<% } %>
